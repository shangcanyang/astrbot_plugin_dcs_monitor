{
    "api_base": {
        "type": "string",
        "title": "DCS API 基础地址",
        "default": "http://119.36.147.45:8041",
        "description": "DCS 服务器的 API 基础地址。"
    },
    "username": {
        "type": "string",
        "title": "登录用户名",
        "default": "J21137",
        "description": "用于登录 DCS 系统的用户名。"
    },
    "password": {
        "type": "string",
        "title": "登录密码",
        "default": "Cxfpass@123",
        "format": "password",
        "description": "用于登录 DCS 系统的密码。"
    },
    "client_id": {
        "type": "string",
        "title": "客户端ID",
        "default": "ms-content-sample",
        "description": "API 登录所需的客户端 ID。"
    },
    "point_prefix": {
        "type": "string",
        "title": "点ID前缀",
        "default": "system:LinkObject:serverdata1:system:",
        "description": "自动拼接到每个点位名称前面的固定前缀。"
    },
    "default_check_interval": {
        "type": "int",
        "title": "全局检查间隔(秒)",
        "default": 10,
        "description": "所有点位的默认检查间隔，单位为秒。"
    },
    "points": {
        "type": "list",
        "title": "监控点位列表（简单模式）",
        "description": "每行一个点位，格式: \"位号,描述,下限,上限\"。下限/上限留空则不预警。例如: HCY_709B101_PV,氨合成B新鲜期流量,0,10000",
        "items": {
            "type": "string",
            "title": "点位配置",
            "description": "格式: \"位号,描述,下限,上限\"。例如: HCY_709B101_PV,氨合成B新鲜期流量,0,10000"
        }
    }
}